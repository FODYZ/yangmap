# GEMINI.md — yangmap

Pointeur : les règles vivent dans `AGENTS.md`, une seule fois, pour tous les
agents (Claude, Codex, Gemini, Hermes).

1. Lis `AGENTS.md` — ce qu'un agent ne doit pas faire dans ce dépôt.
2. Objectif, étape et prochaine action : `pilot open yangmap`, pas ce fichier.
